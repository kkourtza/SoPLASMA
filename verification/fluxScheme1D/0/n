FoamFile { version 2.0; format ascii; class volScalarField; object n; }
dimensions      [0 0 0 0 0 0 0];
internalField   uniform 1;
boundaryField
{
    left          { type fixedValue; value uniform 1.0; }
    right         { type fixedValue; value uniform 2.0; }
    frontAndBack  { type empty; }
}
