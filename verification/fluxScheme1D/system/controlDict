FoamFile { version 2.0; format ascii; class dictionary; object controlDict; }
application     testFluxScheme;

// ROUNDF lives here; it is the PRODUCTION convection scheme in every
// SoPLASMA case, so this bed must be able to select it.
libs ( "libROUNDSchemes.so" );
startFrom       startTime;  startTime 0;
stopAt          endTime;    endTime   1;
deltaT          1;          writeControl runTime; writeInterval 1;
