# SoPlasma (live tree) -- instructions

**THE AUTHORITY IS THE SoEEDF CLAUDE.md.** It is one file governing the whole
multi-module project (SoEEDF + SoPlasma + SoGlobal), it is tracked in the SoEEDF
repo, and it carries the governing directives G1-G3 and the numbered rules.
Imported here rather than copied, so there is exactly one copy to edit --
a second copy WILL go stale (that has already happened once: a session ran with
10 rules when the file had more).

@/home/kkourtza/Projects/SoEEDF/CLAUDE.md

## Local fact: this is the tree the rules mean by "the live SoPlasma tree"

You are in `/home/kkourtza/soplasma-scratch`. Anywhere the imported file says
`../SoPLASMA`, THIS directory is what is meant.
`/home/kkourtza/Projects/SoPLASMA` is a stale clone -- DO NOT EDIT.

## Local fact: the memory knowledge base lives under the SoEEDF project path

Claude Code keys memory to the working directory, so opening this folder gets an
EMPTY memory directory while the whole accumulated knowledge base sits in
`/home/kkourtza/.claude/projects/-home-kkourtza-Projects-SoEEDF/memory/`
(~95 memories, index in its `MEMORY.md`, start with `session-state-*` newest
first). Read that index before starting work here, and write new memories there
too, so the base does not fork.
